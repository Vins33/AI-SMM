# k8s/secret.yaml
# NOTE: In production, use External Secrets Operator or Sealed Secrets
# This is a template - do NOT commit with real values
apiVersion: v1
kind: Secret
metadata:
  name: financial-agent-secrets
  namespace: financial-agent
  labels:
    app.kubernetes.io/name: financial-agent
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Database credentials (replace with real values or use external secrets)
  DATABASE_URL: "postgresql+asyncpg://user:password@postgres-service:5432/financial_db"
  CHECKPOINT_PG_DSN: "postgresql://user:password@postgres-service:5432/financial_db"
  
  # API Keys (replace with real values)
  SERPAPI_API_KEY: "your-serpapi-key-here"
  
  # Service URLs (internal K8s DNS)
  OLLAMA_BASE_URL: "http://ollama-service:11434"
  QDRANT_HOST: "qdrant-service"
